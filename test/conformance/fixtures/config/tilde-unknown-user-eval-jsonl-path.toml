# An unknown user in eval.jsonl_path makes the whole config unusable, so no engine warning is printed.
[engines.grok]
bin = "grok"
args_template = ["-p", "{spec}"]

[eval]
jsonl_path = "~nosuchuserxyz/e.jsonl"
